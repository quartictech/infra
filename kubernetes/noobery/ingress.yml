apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: noobery
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - secretName: tls-noobery
      hosts:
        - noobery.{{cluster.domain_name}}
  rules:
    - host: noobery.{{cluster.domain_name}}
      http:
        paths:
          - path: /
            backend:
              serviceName: metabase
              servicePort: 3000
