apiVersion: v1
kind: Service
metadata:
  name: mock-tracker
  namespace: dilectic
  labels:
    component: mock-tracker
  annotations:
    quartic.io/healthcheck_path: /mock-tracker/healthcheck
    quartic.io/healthcheck_port: "8080"
spec:
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
  selector:
    component: mock-tracker
---
kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: mock-tracker
  namespace: dilectic
spec:
  replicas: 1
  template:
    metadata:
      labels:
        component: mock-tracker
    spec:
      containers:
        - name: tayo
          image: eu.gcr.io/quartictech/mock-tracker:33
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 100m
