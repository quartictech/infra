apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: dilectic
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - secretName: tls-tracker
      hosts:
        - tracker.{{cluster.domain_name}}
  rules:
    - host: tracker.{{cluster.domain_name}}
      http:
        paths:
          - path: /upload
            backend:
              serviceName: tracker
              servicePort: 8080
