apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  namespace: platform
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
    ingress.kubernetes.io/auth-type: basic
    ingress.kubernetes.io/auth-secret: basic-auth
spec:
  tls:
    - secretName: tls-foo
      hosts:
        - alpha.quartic.io
  rules:
    - host: alpha.quartic.io
      http:
        paths:
          - backend:
              serviceName: weyl
              servicePort: 8080
